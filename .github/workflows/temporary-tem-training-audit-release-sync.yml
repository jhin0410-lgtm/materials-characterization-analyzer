name: Register TEM training-audit release sync

on:
  pull_request:
    paths:
      - ".github/workflows/temporary-tem-training-audit-release-sync.yml"

permissions:
  contents: read

jobs:
  register:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm workflow registration
        run: echo "release sync workflow registered"
